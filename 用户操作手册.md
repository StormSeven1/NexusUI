# 多模态自主协同融合控制系统 — 用户操作手册

> 本手册从**界面操作**角度说明如何使用 NexusUI。界面产品名为「多模态自主协同融合控制系统」。

---

## 目录

1. [系统概述与通用操作](#1-系统概述与通用操作)
2. [态势模块](#2-态势模块)
3. [光电模块](#3-光电模块)
4. [告警模块](#4-告警模块)
5. [其他功能](#5-其他功能)
6. [常用操作流程速查](#6-常用操作流程速查)

---

## 1. 系统概述与通用操作

### 1.1 界面布局

打开系统后进入态势主界面，从上到下、从左到右大致为：

| 区域 | 作用 |
|------|------|
| **顶栏** | 产品名、分类菜单（态势/目标/光电/无人机/智能/系统）、快捷按钮、系统模式、气象与时钟、用户菜单 |
| **工作区条** | 标题「态势工作区」、跟踪/告警统计、图层快捷按钮、量算工具 |
| **左侧工具栏** | 快速打开：目标列表、资产列表、图层管理、告警、显示控制 |
| **中央地图** | 态势主视图（航迹、资产、区域、视场等） |
| **右侧栏** | 目标档案、系统评估、智能助手等面板 |
| **底栏** | 数据连接状态、鼠标经纬度、比例尺、缩放级别（如 `Z12`） |

面板可拖动、缩放；多数面板也可从顶栏对应菜单再次打开。

### 1.2 顶栏菜单一览

| 分类 | 子项 | 做什么 |
|------|------|--------|
| **态势** | 图层 | 打开「图层管理」 |
| | 显示 | 打开「显示控制」 |
| **目标** | 目标列表 | 打开航迹列表 |
| | 目标档案 | 打开右侧目标详情 |
| **光电** | 新建光电窗口 | 打开光电视频浮窗 |
| | 运动参数 | 配置光电运动/对准参数 |
| | 停止全部相机任务 | 取消所有相机正在执行的 PTZ 任务 |
| **无人机** | 无人机飞行设置 | 设置飞行速度、高度等 |
| | 一键热备 / 一键取消热备 / 一键返航 | 对全部机场批量下发 |
| **智能** | 智能助手 | 打开对话与值班助手 |
| **系统** | 告警 / 系统评估 / 生成评估报告 / 资产列表 / 数据状态 / 布局 / 软件组成 | 告警与运维相关 |

### 1.3 顶栏快捷按钮

| 按钮 | 操作说明 |
|------|----------|
| **日常查证** | 启停自主值班查证；启动时会打开智能助手 |
| **探鸟采集** | 启停探鸟雷达采集（需有无人机真值） |
| **截屏** | 截取当前系统画面为 PNG |
| **录屏** | 系统级录屏（WebM）；录制中按钮显示「录屏中」 |
| **系统功能** | 快捷入口：新建光电窗口、无人机设置、停止全部相机任务 |

### 1.4 系统模式

顶栏可切换：**平时 / 紧急 / 调试 / 战时**。按值班要求选择对应模式即可。

### 1.5 气象信息

顶栏右侧（时钟附近）显示实时气象条，较宽屏幕下可见。

| 显示项 | 含义 |
|--------|------|
| **天气** | 当前天气概况（如晴、降雨描述等） |
| **气温** | 当前气温（℃） |
| **风速** | 风速（m/s） |
| **降雨** | 降雨状况（超宽屏下显示） |

**操作：**

1. 打开系统后自动加载；加载中显示「气象加载中…」，上游不可用时显示「气象不可用」。
2. 点击气象条右侧的 **ⓘ 详细气象信息** 按钮，弹出「气象详细信息」面板。
3. 面板展示三路实时摘要（约每 10 分钟刷新）：
   - **气象站 · Vaisala**（气温、风速等主数据通常以此为准）
   - **浮标气象**
   - **机场气象**
4. 顶栏降雨可能按优先级回退（气象站 → 浮标 → 机场）；面板会注明「顶栏降雨来自 xxx」。
5. 再点 ⓘ 或面板关闭按钮可关掉详情。

说明：智能助手对话中查询天气时，也可能在聊天区出现天气卡片，与顶栏气象条相互独立。

### 1.6 地图基础操作

| 操作 | 方法 |
|------|------|
| 平移地图 | 鼠标左键拖拽 |
| 缩放 | 鼠标滚轮；底栏显示当前缩放级 |
| 测距 | 工作区条点 **量算** → 左键加点 → 双击结束；右键清空 |
| 测角 | 工作区条点 **角度** → 左键设原点 → 移动看方位/距离 → 再左键结束；右键清空 |
| 绘制区域 | 工作区条点 **区域**（见 [2.2 区域](#22-区域)） |
| 空白处右键 | 弹出 GIS 菜单：光电看向、无人机侦察、雷达采集等 |
| 空白处双击 | 对可用光电下发「看向该位置」 |
| 图例 | 地图右下角折叠「图例」，可查看航迹类型颜色等 |

量算工具激活时，地图业务右键菜单会被暂时屏蔽，先退出量算再操作右键菜单。

### 1.7 图层管理（显隐总控）

**入口**：顶栏 **态势 → 图层**，或左侧工具栏「图层管理」。

面板分四块：

1. **地图** — 底图样式、矢量/栅格子层  
2. **目标图层** — 总开关 + 各类航迹（对海融合、对空融合、探鸟、码头雷达、AIS 等）  
3. **实体图层** — 无人机、雷达、光电、电侦、激光、TDOA、距离环；设备下还可开关「GIS 图标 / 视场 / 能力 / 航线 / 机场」  
4. **区域图层** — 全部区域分组树、固定目标  

看不到某类目标或设备时，优先检查这里对应开关是否打开。

### 1.8 工作区快捷图层

「态势工作区」条上有快捷按钮：

| 按钮 | 作用 |
|------|------|
| **能力** | 隐藏航迹，突出显示在线雷达能力扫描与指定光电能力（再点可取消） |
| **对海雷达** | 显示远遥码头与靖子头等对海雷达航迹 |
| **对海融合** | 显示对海融合航迹 |
| **对空融合** | 显示对空融合航迹 |

条上统计：**跟踪航迹**、**告警事件**、可点击的 **活跃无人机**。

### 1.9 用户与软件组成

- 顶栏右侧圆形用户按钮可打开菜单（含「登出」项；以现场部署是否启用为准）。
- **系统 → 软件组成**：跳转到实体管理、任务管理、作战管理、雷达/相机/无人机/告警/数据管理等子系统（外链）。
- **系统 → 布局**：保存或恢复窗口布局。
- **系统 → 数据状态**：查看各数据源接收间隔，用于排查是否断流。

---

## 2. 态势模块

### 2.1 航迹（目标）

#### 打开目标列表

- 顶栏 **目标 → 目标列表**
- 或左侧工具栏「目标」图标

列表分为 **告警关联目标** 与 **其他目标**。每行通常显示：名称、显示 ID、经纬度、速度、航向（空中目标含高度）。

#### 基本操作

| 你想做 | 怎么做 |
|--------|--------|
| 定位到目标 | 单击列表行 → 地图飞到该目标 |
| 选中地图上的目标 | 单击地图上的航迹符号 |
| 查看详细档案 | 双击地图航迹，或顶栏 **目标 → 目标档案** |
| 搜索目标 | 列表上方搜索框，按名称/ID 关键词过滤 |
| 只看收藏 | 点列表星标按钮 |

**目标档案**（右侧）展示：类型、距离、方位、速度、航向、高度，以及查证相册。空态时提示「地图双击航迹查看档案」。

#### 地图右键（航迹菜单）

在航迹符号上 **右键**，常用项：

| 菜单项 | 说明 |
|--------|------|
| 选择光电跟踪此目标 | 指定光电对该目标跟踪/关注 |
| 无人机跟踪此目标 / 选择无人机跟踪此目标 | 无人机协同跟踪 |
| **历史航迹** | 查看该目标一段时间的历史点迹 |
| 标为无人机 / 取消无人机标记 | 对空融合航迹可用 |
| 设置敌我属性 | 不明 / 红方 / 蓝方 / 白方 |
| 设置目标类型 | 对海：船 / 浮标 / 其他 |
| 自动/手动采集对空雷达数据 | 符合条件时出现 |

说明：设为 **蓝方** 往往会连带确认相关告警；设为其他敌我属性可能取消告警（以现场逻辑为准）。

#### 历史航迹

1. 右键航迹 → **历史航迹**  
2. 浮层标题类似「`xxx`目标历史航迹」  
3. 用滑条调节回溯 **分钟数**（最长约 120 分钟）  
4. 可打开 **常显**，关闭面板后地图仍保留历史绘制  
5. 按 `Esc` 或点关闭按钮退出面板  

#### 航迹显示样式

**入口**：顶栏 **态势 → 显示** → Tab「航迹显示」。

可按航迹类型调整：

- 颜色（对空融合可区分鸟 / 无人机）
- **矢量长度**（秒）
- **尾迹长度**（秒）；可「应用到全部类型」

图层显隐仍在「图层管理 → 目标图层」中控制。

---

### 2.2 区域

区域数据包括 **区域面/航线** 与 **固定目标**，显示在地图上，并在图层管理中控制显隐。

#### 绘制区域 / 航线 / 固定目标

1. 在工作区条点击 **区域**，打开「绘制区域」对话框。  
2. 选择类型：
   - **区域 / 航线**
   - **固定目标**
3. 选择或新建 **分组**（航线使用固定分组规则）。  
4. 选择形状：
   - **矩形**：左键两点对角  
   - **圆形**：圆心 + 圆周点  
   - **多边形**：左键加点，双击闭合  
   - **航线**：左键折点，双击结束（固定目标不可选航线）  
5. 点 **开始绘制**，地图出现十字光标；**右键可取消**。  
6. 绘制完成后弹出保存框，填写名称后入库。

对话框右侧可展开「区域 / 航线」树，悬停可删除。

#### 删除区域

| 方式 | 步骤 |
|------|------|
| 对话框树 | 悬停条目 → 垃圾桶 → 确认删除 |
| 地图上 | 在区域/航线上 **右键** → 删除区域 / 删除航线 → 确认 |

#### 显隐与样式

- **显隐**：图层管理 → **区域图层** → 「全部区域」总开关 → 展开分组开关单个区域；同级可开关「固定目标」。  
- **边线样式**：显示控制 → **态势显示**，可调区域边线颜色、透明度、线型、线宽、标签透明度（全局样式）。

#### 航线与无人机

在地图航线上右键，可出现 **无人机按航线飞行** 等项（有无人机资源时）。

---

### 2.3 资产

资产指雷达、相机（光电）、无人机/机场及其他设备。

#### 打开资产列表

- 顶栏 **系统 → 资产列表**
- 或左侧工具栏「资产」图标

#### 列表操作

1. 点 **刷新**（列表也会定时刷新）。  
2. 可勾选 **仅显示在线设备**。  
3. 用 **搜索资产** 按名称过滤。  
4. 树形分类浏览：**HOME / 雷达 / 相机 / 无人机 / 其他设备**。  
5. 行上可见：名称、在线/离线/降级状态、ID、坐标等。  
6. **单击资产行**：选中并（有坐标时）地图飞到该设备；再点可取消选中。

资产在地图上的专题层（图标、视场、能力环、航线、机场等）在 **图层管理 → 实体图层** 中按设备展开开关。

---

## 3. 光电模块

光电模块覆盖：相机视频窗口、云台（PTZ）、检测框跟踪、无人机画面与控制，以及与态势地图的联动。

### 3.1 打开与管理光电窗口

**入口**：

- 顶栏 **光电 → 新建光电窗口**
- 或顶栏 **系统功能 → 新建光电窗口**

说明：

- 每次打开一个可拖动的视频浮窗（最多约 10 路，满员时提示「光电窗口已满」）。  
- **点击窗口标题栏/内容** 可选中该窗（边框高亮），便于态势联动与键盘手控绑定到当前窗口。

### 3.2 切换视频源（海上 / 对空 / 无人机等）

在视频画面上 **右键**，按分组选择流：

- **光电** — 各站点可见光、红外、广角等（如靖子头、远遥码头等）
- **无人机** — UAV 画面
- **第三方相机** — 第三方接入流

现场默认对海/对空机位由部署配置决定（开发现场与正式现场可能不同），以右键菜单中实际可选流名为准。

**画中画**：窗口右侧工具栏点「画中画」，可同时观看副路视频。

### 3.3 相机云台（PTZ）控制

1. 在光电窗右侧工具中打开 **相机云台控制**。  
2. 常用操作：

| 操作 | 方法 |
|------|------|
| 方位俯仰 | 方向键 **按住移动，松开停止** |
| 变倍 | 变倍 +/− 按住；或在画面上 **滚轮** |
| 对焦 | 对焦 +/− 按住 |
| 相对拖拽 | 在画面上拖动进行相对 PTZ |

其他相关：

- **光电 → 停止全部相机任务**：紧急取消所有相机 PTZ 任务  
- **光电 → 运动参数**：调整运动/对准参数  
- 放大窗口后工具栏还有 PID、对准采集、跟踪采集等高级项（按权限与现场开放情况使用）

### 3.4 检测框与图像跟踪

检测框由后端检测数据自动叠加在视频上，无需单独「开框」菜单。

| 操作 | 效果 |
|------|------|
| **双击检测框** | 发起对该目标的图像跟踪 |
| 跟踪中再双击 | 取消跟踪 |
| （无人机）仅播放视频 | 可关闭检测叠加，用于排查卡顿 |

若提示「当前无检测框无法发起跟踪」，说明此刻没有可用检测结果，需等待检测或检查链路。

### 3.5 无人机操作

#### 从顶栏批量控制

顶栏 **无人机**：

| 菜单 | 说明 |
|------|------|
| 无人机飞行设置 | 设置飞行速度 (m/s)、飞行高度 (m) 并保存 |
| 一键热备 | 全部机场热备 |
| 一键取消热备 | 取消热备 |
| 一键返航 | 全部机场依次返航 |

#### 在光电窗内控制

1. 右键视频 → **无人机** 分组选择 UAV 流。  
2. 打开右侧 **无人机控制台**。  
3. 控制台常用按钮：**起飞、中止、返航、热备、重连、急停**。  
4. **键盘手控**（需选中该光电窗）：`Q W E A S D Z C`（控制台会高亮提示）。  
5. 画面操作：
   - 拖动松手：瞄准（camera_aim）  
   - 双击检测框：图像跟踪 / 再双击取消  
   - 滚轮：变倍  
6. 载荷相关（以界面实际按钮为准）：云台回中、云台向下、喊话（按住录音）、探照灯模式等。  
7. 可用镜头切换条切换无人机镜头。

### 3.6 光电与态势联动

| 地图操作 | 光电/无人机效果 |
|----------|-----------------|
| **双击航迹** | 打开目标档案，并下发光电关注/看向等任务（具体是否跟踪由现场配置决定） |
| **双击无人机自报位图标** | 类似融合航迹的相机跟踪链路 |
| 右键航迹 → **选择光电跟踪此目标** | 指定光电跟踪该目标 |
| 右键航迹 → **无人机跟踪此目标** | 无人机跟踪该目标 |
| 右键空白 → **选择光电看向此位置** | 光电转到该地理点 |
| 右键空白 → **无人机侦察此位置** / 选择无人机侦察 | 无人机去该点侦察 |
| 空白处 **双击** | 对可用光电看向该位置 |
| 告警列表点航迹告警 | 选中航迹并飞到位置，再配合双击/右键做光电查证 |

**推荐查证路径**：告警中心点目标 → 地图定位 → 双击航迹或右键「选择光电跟踪」→ 光电窗查看检测框 → 双击框精细跟踪。

### 3.7 光电窗内录像与截图

| 功能 | 说明 |
|------|------|
| 截图 | 保存当前视频帧为 PNG |
| 开始/停止录屏 | 录制当前光电窗视频 |
| 重连：重新拉流 | 视频中断时重新拉流 |
| 放大窗口 / 恢复默认 | 放大后工具更全 |

说明：系统顶栏「截屏/录屏」是整屏级；光电窗内是针对当前视频。

---

## 4. 告警模块

### 4.1 打开告警中心

- 顶栏 **系统 → 告警**
- 或左侧工具栏「告警」图标  

面板标题为 **告警中心**。顶部可见类似「N 条严重 · M 条」的统计；无数据时显示「暂无告警」。

### 4.2 阅读告警

每条告警一般包含：

- **级别**：严重 / 警告 / 信息 / 相机等  
- **时间**、目标号、对海/对空标识、摘要  
- 航迹类常见状态文案：**威胁**（未确认）、**告警**（已确认）  
- 鼠标悬停可查看证据链信息（有证据时）

**点击整行**（航迹/相机检测类）：选中对应航迹并地图飞到位置。

### 4.3 筛选

告警中心提供本地筛选（选择会记住）：

| 维度 | 选项示例 |
|------|----------|
| 类型 | 全部、航迹、装备状态、数据通信、任务、环境、综合、系统权鉴 |
| 级别 | 全部、严重、警告、信息 |
| 系统 | 全部、航迹、融控、作战管理、实体/任务/相机/无人机/告警/数据/雷达管理等 |

### 4.4 确认、查证与删除

| 按钮 | 适用 | 作用 |
|------|------|------|
| **确认告警** | 未确认的航迹威胁 | 确认为告警 |
| **查证** | 航迹告警 | 走 GIS 查证链路（联动光电等） |
| **删除** | 航迹或系统告警 | 关闭/取消该条告警 |

地图上将目标设为 **蓝方**，效果上接近确认告警。

### 4.5 语音播报

系统会对告警进行语音提示（每条通常只播报一次）。首次使用需先在页面内点击或按键，以允许浏览器播放声音。  
播报文案示例：「距离 xx 海里，方位 xx 度发现威胁目标，目标尾号 xxxx」。

---

## 5. 其他功能

### 5.1 智能助手

**入口**：顶栏 **智能 → 智能助手**，或右侧栏机器人图标；启动「日常查证」时也会自动打开。

面板内常见 Tab：

- **值班助手** — 开启自主值班后，查证进度写入此 Tab  
- **AI 助手** — 智能对话与指令调度  
- **会话 N** — 工作流会话  

可切换 **智能助手 / 知识库** 模式。常见操作：新建会话、清空对话、停止工作流、停止采集、确认/取消中断。  
也可使用界面提供的快捷问题（如出动相机/无人机找船、统计海里内目标、无人机搜索/返航、探鸟采集等）。

### 5.2 日常查证与探鸟采集

| 顶栏按钮 | 说明 |
|----------|------|
| **日常查证** | 启停自主值班查证；停止时会结束工作流并停相关光电查证 |
| **探鸟采集** | 有无人机真值时开始采集；超时无真值会自动停止 |

### 5.3 系统评估与评估报告

- **系统 → 系统评估**：查看系统评估、航迹评估、相机评估、算法评估等 Tab。  
- **系统 → 生成评估报告**：生成并预览评估报告。

### 5.4 显示控制（距离环等）

**态势 → 显示**：

- **航迹显示**：颜色、矢量、尾迹（见 2.1）  
- **态势显示**：距离环、区域图层边线样式等  

### 5.5 布局与数据状态

- **系统 → 布局**：保存当前多窗口布局，或恢复已保存布局。  
- **系统 → 数据状态**：检查航迹、资产、告警等数据是否正常刷新，断流时优先看这里。

### 5.6 图例

地图右下角打开 **图例**，对照航迹类型颜色、重点关注/威胁等标识。

### 5.7 快捷键速览

| 场景 | 按键 | 作用 |
|------|------|------|
| 无人机控制台打开且光电窗已选中 | `Q` `W` `E` `A` `S` `D` `Z` `C` | 无人机手控 |
| 历史航迹面板 / 多数对话框 | `Esc` | 关闭 |
| 光电视频画面 | 滚轮 | 变倍 |
| 光电视频画面 | 拖拽 | PTZ / 无人机瞄准 |
| 光电视频画面 | 双击检测框 | 跟踪 / 取消跟踪 |

---

## 6. 常用操作流程速查

### 流程 A：发现威胁 → 光电确认

1. 打开 **告警中心**，点击目标告警行，地图定位。  
2. **双击**地图航迹，或右键 **选择光电跟踪此目标**。  
3. 打开/切到对应 **光电窗口**，观察检测框。  
4. 需要时 **双击检测框** 精细跟踪。  
5. 确认后点 **确认告警**，或地图右键设为 **蓝方**；误报则 **删除**。

### 流程 B：打开对海/对空视频并手控

1. **光电 → 新建光电窗口**。  
2. 视频区右键，选择对应站点可见光/红外流。  
3. 打开 **云台控制**，方向键/滚轮/拖拽调节。  
4. 紧急情况用 **停止全部相机任务**。

### 流程 C：无人机出动与返航

1. **无人机 → 无人机飞行设置** 确认速度、高度。  
2. 光电窗切到无人机流，打开控制台，执行 **热备 → 起飞**（或顶栏一键热备）。  
3. 地图右键目标选 **无人机跟踪此目标**，或右键空白选侦察位置。  
4. 任务结束：**返航** 或顶栏 **一键返航**；紧急用控制台 **急停/中止**。

### 流程 D：标绘管控区域

1. 工作区条点 **区域**。  
2. 选类型与形状 → **开始绘制** → 地图上完成图形 → 命名保存。  
3. 在 **图层管理 → 区域图层** 中确认已显示。  
4. 不需要时在树上或地图右键删除。

### 流程 E：只看某类航迹

1. 工作区快捷按钮点 **对海融合 / 对空融合 / 对海雷达**，或  
2. 打开 **图层管理 → 目标图层**，关闭无关子类、打开需要的子类。  
3. 需要改颜色/尾迹时到 **态势 → 显示 → 航迹显示**。

---

*文档依据当前 NexusUI 界面整理。若现场菜单文案或默认机位与本文略有差异，以实际界面为准。*
