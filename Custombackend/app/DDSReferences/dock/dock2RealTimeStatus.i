// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dock2RealTimeStatus.i
 * This header file contains the SWIG interface of the described types in the IDL file.
 *
 * This file was generated by the tool fastddsgen.
 */

%module(moduleimport="if __import__('os').name == 'nt': import win32api; win32api.LoadLibrary('dock2RealTimeStatus.dll')\nif __package__ or '.' in __name__:\n    from . import _dock2RealTimeStatusWrapper\nelse:\n    import _dock2RealTimeStatusWrapper") dock2RealTimeStatus

// If using windows in debug, it would try to use python_d, which would not be found.
%begin %{
#ifdef _MSC_VER
#define SWIG_PYTHON_INTERPRETER_NO_DEBUG
#endif
#include <exception>
%}

// SWIG helper modules
%include "stdint.i"
%include "std_array.i"
%include "std_map.i"
%include "std_string.i"
%include "std_vector.i"
%include "typemaps.i"

// Assignemt operators are ignored, as there is no such thing in Python.
// Trying to export them issues a warning
%ignore *::operator=;

// Macro declarations
// Any macro used on the Fast DDS header files will give an error if it is not redefined here
#define FASTDDS_EXPORTED_API
#define eProsima_user_DllExport


%{
#include "dock2RealTimeStatus.hpp"

#include <fastdds/dds/core/LoanableSequence.hpp>
%}

%include <fastcdr/config.h>
%import(module="fastdds") "fastcdr/xcdr/optional.hpp"
%import(module="fastdds") "fastdds/dds/core/LoanableCollection.hpp"
%import(module="fastdds") "fastdds/dds/core/LoanableTypedCollection.hpp"
%import(module="fastdds") "fastdds/dds/core/LoanableSequence.hpp"

%define %traits_penumn(Type...)
  %fragment(SWIG_Traits_frag(Type),"header",
        fragment="StdTraits") {
namespace swig {
  template <> struct traits< Type > {
    typedef value_category category;
    static const char* type_name() { return  #Type; }
  };
}
}
%enddef

%traits_penumn(enum casia::device::status::dock2Status::HomePositionValidity);
%traits_penumn(enum casia::device::status::dock2Status::RTCMDeviceType);
%traits_penumn(enum casia::device::status::dock2Status::CalibrationType);
%traits_penumn(enum casia::device::status::dock2Status::AirConditionerState);
%traits_penumn(enum casia::device::status::dock2Status::SilentMode);
%traits_penumn(enum casia::device::status::dock2Status::UserExperienceImprovement);
%traits_penumn(enum casia::device::status::dock2Status::DongleType);
%traits_penumn(enum casia::device::status::dock2Status::ESIMActivationState);
%traits_penumn(enum casia::device::status::dock2Status::SIMCardState);
%traits_penumn(enum casia::device::status::dock2Status::SIMSlot);
%traits_penumn(enum casia::device::status::dock2Status::TelecomOperator);
%traits_penumn(enum casia::device::status::dock2Status::SIMType);
%traits_penumn(enum casia::device::status::dock2Status::MaintenanceState);
%traits_penumn(enum casia::device::status::dock2Status::HeatState);
%traits_penumn(enum casia::device::status::dock2Status::SatelliteQuality);
%traits_penumn(enum casia::device::status::dock2Status::EmergencyStopState);
%traits_penumn(enum casia::device::status::dock2Status::ChargingState);
%traits_penumn(enum casia::device::status::dock2Status::BatteryStoreMode);
%traits_penumn(enum casia::device::status::dock2Status::FirmwareConsistency);
%traits_penumn(enum casia::device::status::dock2Status::Rainfall);
%traits_penumn(enum casia::device::status::dock2Status::VideoQuality);
%traits_penumn(enum casia::device::status::dock2Status::LiveStreamingState);
%traits_penumn(enum casia::device::status::dock2Status::LinkState);
%traits_penumn(enum casia::device::status::dock2Status::LinkWorkMode);
%traits_penumn(enum casia::device::status::dock2Status::NetworkType);
%traits_penumn(enum casia::device::status::dock2Status::NetworkQuality);
%traits_penumn(enum casia::device::status::dock2Status::SupplementaryLightState);
%traits_penumn(enum casia::device::status::dock2Status::CoverState);
%traits_penumn(enum casia::device::status::dock2Status::DeviceOnlineStatus);
%traits_penumn(enum casia::device::status::dock2Status::DevicePairedStatus);
%traits_penumn(enum casia::device::status::dock2Status::FlightTaskStepCode);
%traits_penumn(enum casia::device::status::dock2Status::DockModeCode);
%traits_penumn(enum casia::device::status::dock2Status::FirmwareUpgradeStatus);
////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::RTCMInfo
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::RTCMInfo::RTCMInfo(casia::device::status::dock2Status::RTCMInfo&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::RTCMInfo::mount_point(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::RTCMInfo::mount_point();
%rename("%s") casia::device::status::dock2Status::RTCMInfo::mount_point() const;



%ignore casia::device::status::dock2Status::RTCMInfo::port(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::RTCMInfo::port();
%rename("%s") casia::device::status::dock2Status::RTCMInfo::port() const;



%ignore casia::device::status::dock2Status::RTCMInfo::host(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::RTCMInfo::host();
%rename("%s") casia::device::status::dock2Status::RTCMInfo::host() const;



%ignore casia::device::status::dock2Status::RTCMInfo::rtcm_device_type(casia::device::status::dock2Status::RTCMDeviceType&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::RTCMInfo::rtcm_device_type();
%rename("%s") casia::device::status::dock2Status::RTCMInfo::rtcm_device_type() const;



%ignore casia::device::status::dock2Status::RTCMInfo::source_type(casia::device::status::dock2Status::CalibrationType&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::RTCMInfo::source_type();
%rename("%s") casia::device::status::dock2Status::RTCMInfo::source_type() const;



%template(_RTCMInfoSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::RTCMInfo, std::false_type>;
%template(RTCMInfoSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::RTCMInfo, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::RTCMInfo, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::RTCMInfo& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::CenterNode
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::CenterNode::CenterNode(casia::device::status::dock2Status::CenterNode&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::CenterNode::sdr_id(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::CenterNode::sdr_id();
%rename("%s") casia::device::status::dock2Status::CenterNode::sdr_id() const;



%ignore casia::device::status::dock2Status::CenterNode::sn(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::CenterNode::sn();
%rename("%s") casia::device::status::dock2Status::CenterNode::sn() const;



%template(_CenterNodeSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::CenterNode, std::false_type>;
%template(CenterNodeSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::CenterNode, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::CenterNode, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::CenterNode& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::LeafNode
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::LeafNode::LeafNode(casia::device::status::dock2Status::LeafNode&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::LeafNode::sdr_id(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LeafNode::sdr_id();
%rename("%s") casia::device::status::dock2Status::LeafNode::sdr_id() const;



%ignore casia::device::status::dock2Status::LeafNode::sn(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LeafNode::sn();
%rename("%s") casia::device::status::dock2Status::LeafNode::sn() const;



%ignore casia::device::status::dock2Status::LeafNode::control_source_index(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LeafNode::control_source_index();
%rename("%s") casia::device::status::dock2Status::LeafNode::control_source_index() const;



%template(_LeafNodeSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::LeafNode, std::false_type>;
%template(LeafNodeSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LeafNode, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LeafNode, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::LeafNode& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::WirelessLinkTopo
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::WirelessLinkTopo::WirelessLinkTopo(casia::device::status::dock2Status::WirelessLinkTopo&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::WirelessLinkTopo::secret_code(std::vector<uint32_t>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLinkTopo::secret_code() const;
%extend std::vector<uint32_t>
{
    const uint32_t* get_buffer() const
    {
        return self->data();
    }
}

%template(uint32_t_vector) std::vector<uint32_t>;


%ignore casia::device::status::dock2Status::WirelessLinkTopo::center_node(casia::device::status::dock2Status::CenterNode&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLinkTopo::center_node();
%rename("%s") casia::device::status::dock2Status::WirelessLinkTopo::center_node() const;



%ignore casia::device::status::dock2Status::WirelessLinkTopo::leaf_nodes(std::vector<casia::device::status::dock2Status::LeafNode>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLinkTopo::leaf_nodes() const;
%template(LeafNode_vector) std::vector<casia::device::status::dock2Status::LeafNode>;


%template(_WirelessLinkTopoSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::WirelessLinkTopo, std::false_type>;
%template(WirelessLinkTopoSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::WirelessLinkTopo, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::WirelessLinkTopo, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::WirelessLinkTopo& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::AirConditioner
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::AirConditioner::AirConditioner(casia::device::status::dock2Status::AirConditioner&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::AirConditioner::air_conditioner_state(casia::device::status::dock2Status::AirConditionerState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AirConditioner::air_conditioner_state();
%rename("%s") casia::device::status::dock2Status::AirConditioner::air_conditioner_state() const;



%ignore casia::device::status::dock2Status::AirConditioner::switch_time(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AirConditioner::switch_time();
%rename("%s") casia::device::status::dock2Status::AirConditioner::switch_time() const;



%template(_AirConditionerSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::AirConditioner, std::false_type>;
%template(AirConditionerSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::AirConditioner, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::AirConditioner, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::AirConditioner& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::ESIMInfo
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::ESIMInfo::ESIMInfo(casia::device::status::dock2Status::ESIMInfo&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::ESIMInfo::telecom_operator(casia::device::status::dock2Status::TelecomOperator&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::ESIMInfo::telecom_operator();
%rename("%s") casia::device::status::dock2Status::ESIMInfo::telecom_operator() const;



%ignore casia::device::status::dock2Status::ESIMInfo::enabled(bool&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::ESIMInfo::enabled();
%rename("%s") casia::device::status::dock2Status::ESIMInfo::enabled() const;



%ignore casia::device::status::dock2Status::ESIMInfo::iccid(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::ESIMInfo::iccid();
%rename("%s") casia::device::status::dock2Status::ESIMInfo::iccid() const;



%template(_ESIMInfoSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::ESIMInfo, std::false_type>;
%template(ESIMInfoSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::ESIMInfo, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::ESIMInfo, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::ESIMInfo& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::SIMInfo
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::SIMInfo::SIMInfo(casia::device::status::dock2Status::SIMInfo&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::SIMInfo::telecom_operator(casia::device::status::dock2Status::TelecomOperator&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SIMInfo::telecom_operator();
%rename("%s") casia::device::status::dock2Status::SIMInfo::telecom_operator() const;



%ignore casia::device::status::dock2Status::SIMInfo::sim_type(casia::device::status::dock2Status::SIMType&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SIMInfo::sim_type();
%rename("%s") casia::device::status::dock2Status::SIMInfo::sim_type() const;



%ignore casia::device::status::dock2Status::SIMInfo::iccid(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SIMInfo::iccid();
%rename("%s") casia::device::status::dock2Status::SIMInfo::iccid() const;



%template(_SIMInfoSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::SIMInfo, std::false_type>;
%template(SIMInfoSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::SIMInfo, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::SIMInfo, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::SIMInfo& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::DongleInfo
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::DongleInfo::DongleInfo(casia::device::status::dock2Status::DongleInfo&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::DongleInfo::imei(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::imei();
%rename("%s") casia::device::status::dock2Status::DongleInfo::imei() const;



%ignore casia::device::status::dock2Status::DongleInfo::dongle_type(casia::device::status::dock2Status::DongleType&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::dongle_type();
%rename("%s") casia::device::status::dock2Status::DongleInfo::dongle_type() const;



%ignore casia::device::status::dock2Status::DongleInfo::eid(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::eid();
%rename("%s") casia::device::status::dock2Status::DongleInfo::eid() const;



%ignore casia::device::status::dock2Status::DongleInfo::esim_activate_state(casia::device::status::dock2Status::ESIMActivationState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::esim_activate_state();
%rename("%s") casia::device::status::dock2Status::DongleInfo::esim_activate_state() const;



%ignore casia::device::status::dock2Status::DongleInfo::sim_card_state(casia::device::status::dock2Status::SIMCardState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::sim_card_state();
%rename("%s") casia::device::status::dock2Status::DongleInfo::sim_card_state() const;



%ignore casia::device::status::dock2Status::DongleInfo::sim_slot(casia::device::status::dock2Status::SIMSlot&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::sim_slot();
%rename("%s") casia::device::status::dock2Status::DongleInfo::sim_slot() const;



%ignore casia::device::status::dock2Status::DongleInfo::esim_infos(std::vector<casia::device::status::dock2Status::ESIMInfo>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::esim_infos() const;
%template(ESIMInfo_vector) std::vector<casia::device::status::dock2Status::ESIMInfo>;


%ignore casia::device::status::dock2Status::DongleInfo::sim_info(casia::device::status::dock2Status::SIMInfo&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DongleInfo::sim_info();
%rename("%s") casia::device::status::dock2Status::DongleInfo::sim_info() const;



%template(_DongleInfoSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::DongleInfo, std::false_type>;
%template(DongleInfoSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DongleInfo, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DongleInfo, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::DongleInfo& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::Battery
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::Battery::Battery(casia::device::status::dock2Status::Battery&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::Battery::capacity_percent(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::Battery::capacity_percent();
%rename("%s") casia::device::status::dock2Status::Battery::capacity_percent() const;



%ignore casia::device::status::dock2Status::Battery::index(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::Battery::index();
%rename("%s") casia::device::status::dock2Status::Battery::index() const;



%ignore casia::device::status::dock2Status::Battery::voltage(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::Battery::voltage();
%rename("%s") casia::device::status::dock2Status::Battery::voltage() const;



%ignore casia::device::status::dock2Status::Battery::temperature(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::Battery::temperature();
%rename("%s") casia::device::status::dock2Status::Battery::temperature() const;



%template(_BatterySeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::Battery, std::false_type>;
%template(BatterySeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::Battery, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::Battery, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::Battery& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::DroneBatteryMaintenanceInfo
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::DroneBatteryMaintenanceInfo(casia::device::status::dock2Status::DroneBatteryMaintenanceInfo&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::maintenance_state(casia::device::status::dock2Status::MaintenanceState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::maintenance_state();
%rename("%s") casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::maintenance_state() const;



%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::maintenance_time_left(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::maintenance_time_left();
%rename("%s") casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::maintenance_time_left() const;



%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::heat_state(casia::device::status::dock2Status::HeatState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::heat_state();
%rename("%s") casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::heat_state() const;



%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::batteries(std::vector<casia::device::status::dock2Status::Battery>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DroneBatteryMaintenanceInfo::batteries() const;
%template(Battery_vector) std::vector<casia::device::status::dock2Status::Battery>;


%template(_DroneBatteryMaintenanceInfoSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::DroneBatteryMaintenanceInfo, std::false_type>;
%template(DroneBatteryMaintenanceInfoSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DroneBatteryMaintenanceInfo, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DroneBatteryMaintenanceInfo, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::DroneBatteryMaintenanceInfo& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

%traits_penumn(enum casia::device::status::dock2Status::IsWithMaintenance);
%traits_penumn(enum casia::device::status::dock2Status::AircraftMaintenanceLevel);
////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::MaintainStatusItem
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::MaintainStatusItem::MaintainStatusItem(casia::device::status::dock2Status::MaintainStatusItem&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::MaintainStatusItem::state(casia::device::status::dock2Status::IsWithMaintenance&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::MaintainStatusItem::state();
%rename("%s") casia::device::status::dock2Status::MaintainStatusItem::state() const;



%ignore casia::device::status::dock2Status::MaintainStatusItem::last_maintain_type(casia::device::status::dock2Status::AircraftMaintenanceLevel&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::MaintainStatusItem::last_maintain_type();
%rename("%s") casia::device::status::dock2Status::MaintainStatusItem::last_maintain_type() const;



%ignore casia::device::status::dock2Status::MaintainStatusItem::last_maintain_time(int64_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::MaintainStatusItem::last_maintain_time();
%rename("%s") casia::device::status::dock2Status::MaintainStatusItem::last_maintain_time() const;



%ignore casia::device::status::dock2Status::MaintainStatusItem::last_maintain_work_sorties(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::MaintainStatusItem::last_maintain_work_sorties();
%rename("%s") casia::device::status::dock2Status::MaintainStatusItem::last_maintain_work_sorties() const;



%template(_MaintainStatusItemSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::MaintainStatusItem, std::false_type>;
%template(MaintainStatusItemSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::MaintainStatusItem, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::MaintainStatusItem, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::MaintainStatusItem& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::MaintainStatus
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::MaintainStatus::MaintainStatus(casia::device::status::dock2Status::MaintainStatus&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::MaintainStatus::maintain_status_array(std::vector<casia::device::status::dock2Status::MaintainStatusItem>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::MaintainStatus::maintain_status_array() const;
%template(MaintainStatusItem_vector) std::vector<casia::device::status::dock2Status::MaintainStatusItem>;


%template(_MaintainStatusSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::MaintainStatus, std::false_type>;
%template(MaintainStatusSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::MaintainStatus, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::MaintainStatus, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::MaintainStatus& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::PositionState
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::PositionState::PositionState(casia::device::status::dock2Status::PositionState&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::PositionState::is_calibration(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::PositionState::is_calibration();
%rename("%s") casia::device::status::dock2Status::PositionState::is_calibration() const;



%ignore casia::device::status::dock2Status::PositionState::is_fixed(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::PositionState::is_fixed();
%rename("%s") casia::device::status::dock2Status::PositionState::is_fixed() const;



%ignore casia::device::status::dock2Status::PositionState::quality(casia::device::status::dock2Status::SatelliteQuality&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::PositionState::quality();
%rename("%s") casia::device::status::dock2Status::PositionState::quality() const;



%ignore casia::device::status::dock2Status::PositionState::gps_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::PositionState::gps_number();
%rename("%s") casia::device::status::dock2Status::PositionState::gps_number() const;



%ignore casia::device::status::dock2Status::PositionState::rtk_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::PositionState::rtk_number();
%rename("%s") casia::device::status::dock2Status::PositionState::rtk_number() const;



%template(_PositionStateSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::PositionState, std::false_type>;
%template(PositionStateSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::PositionState, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::PositionState, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::PositionState& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::DroneChargeState
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::DroneChargeState::DroneChargeState(casia::device::status::dock2Status::DroneChargeState&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::DroneChargeState::capacity_percent(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DroneChargeState::capacity_percent();
%rename("%s") casia::device::status::dock2Status::DroneChargeState::capacity_percent() const;



%ignore casia::device::status::dock2Status::DroneChargeState::state(casia::device::status::dock2Status::ChargingState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DroneChargeState::state();
%rename("%s") casia::device::status::dock2Status::DroneChargeState::state() const;



%template(_DroneChargeStateSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::DroneChargeState, std::false_type>;
%template(DroneChargeStateSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DroneChargeState, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DroneChargeState, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::DroneChargeState& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::BackupBattery
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::BackupBattery::BackupBattery(casia::device::status::dock2Status::BackupBattery&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::BackupBattery::switch_state(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::BackupBattery::switch_state();
%rename("%s") casia::device::status::dock2Status::BackupBattery::switch_state() const;



%ignore casia::device::status::dock2Status::BackupBattery::voltage(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::BackupBattery::voltage();
%rename("%s") casia::device::status::dock2Status::BackupBattery::voltage() const;



%ignore casia::device::status::dock2Status::BackupBattery::temperature(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::BackupBattery::temperature();
%rename("%s") casia::device::status::dock2Status::BackupBattery::temperature() const;



%template(_BackupBatterySeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::BackupBattery, std::false_type>;
%template(BackupBatterySeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::BackupBattery, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::BackupBattery, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::BackupBattery& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::AlternateLandPoint
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::AlternateLandPoint::AlternateLandPoint(casia::device::status::dock2Status::AlternateLandPoint&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::AlternateLandPoint::longitude(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AlternateLandPoint::longitude();
%rename("%s") casia::device::status::dock2Status::AlternateLandPoint::longitude() const;



%ignore casia::device::status::dock2Status::AlternateLandPoint::latitude(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AlternateLandPoint::latitude();
%rename("%s") casia::device::status::dock2Status::AlternateLandPoint::latitude() const;



%ignore casia::device::status::dock2Status::AlternateLandPoint::safe_land_height(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AlternateLandPoint::safe_land_height();
%rename("%s") casia::device::status::dock2Status::AlternateLandPoint::safe_land_height() const;



%ignore casia::device::status::dock2Status::AlternateLandPoint::is_configured(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AlternateLandPoint::is_configured();
%rename("%s") casia::device::status::dock2Status::AlternateLandPoint::is_configured() const;



%ignore casia::device::status::dock2Status::AlternateLandPoint::height(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::AlternateLandPoint::height();
%rename("%s") casia::device::status::dock2Status::AlternateLandPoint::height() const;



%template(_AlternateLandPointSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::AlternateLandPoint, std::false_type>;
%template(AlternateLandPointSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::AlternateLandPoint, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::AlternateLandPoint, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::AlternateLandPoint& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::StorageCapacity
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::StorageCapacity::StorageCapacity(casia::device::status::dock2Status::StorageCapacity&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::StorageCapacity::total(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::StorageCapacity::total();
%rename("%s") casia::device::status::dock2Status::StorageCapacity::total() const;



%ignore casia::device::status::dock2Status::StorageCapacity::used(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::StorageCapacity::used();
%rename("%s") casia::device::status::dock2Status::StorageCapacity::used() const;



%template(_StorageCapacitySeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::StorageCapacity, std::false_type>;
%template(StorageCapacitySeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::StorageCapacity, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::StorageCapacity, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::StorageCapacity& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::VideoList
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::VideoList::VideoList(casia::device::status::dock2Status::VideoList&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::VideoList::video_index(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::VideoList::video_index();
%rename("%s") casia::device::status::dock2Status::VideoList::video_index() const;



%ignore casia::device::status::dock2Status::VideoList::video_type(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::VideoList::video_type();
%rename("%s") casia::device::status::dock2Status::VideoList::video_type() const;



%ignore casia::device::status::dock2Status::VideoList::switchable_video_types(std::vector<std::string>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::VideoList::switchable_video_types() const;
%template(string_vector) std::vector<std::string>;


%template(_VideoListSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::VideoList, std::false_type>;
%template(VideoListSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::VideoList, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::VideoList, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::VideoList& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::CameraList
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::CameraList::CameraList(casia::device::status::dock2Status::CameraList&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::CameraList::camera_index(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::CameraList::camera_index();
%rename("%s") casia::device::status::dock2Status::CameraList::camera_index() const;



%ignore casia::device::status::dock2Status::CameraList::available_video_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::CameraList::available_video_number();
%rename("%s") casia::device::status::dock2Status::CameraList::available_video_number() const;



%ignore casia::device::status::dock2Status::CameraList::coexist_video_number_max(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::CameraList::coexist_video_number_max();
%rename("%s") casia::device::status::dock2Status::CameraList::coexist_video_number_max() const;



%ignore casia::device::status::dock2Status::CameraList::video_list(std::vector<casia::device::status::dock2Status::VideoList>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::CameraList::video_list() const;
%template(VideoList_vector) std::vector<casia::device::status::dock2Status::VideoList>;


%template(_CameraListSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::CameraList, std::false_type>;
%template(CameraListSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::CameraList, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::CameraList, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::CameraList& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::DeviceList
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::DeviceList::DeviceList(casia::device::status::dock2Status::DeviceList&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::DeviceList::sn(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DeviceList::sn();
%rename("%s") casia::device::status::dock2Status::DeviceList::sn() const;



%ignore casia::device::status::dock2Status::DeviceList::available_video_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DeviceList::available_video_number();
%rename("%s") casia::device::status::dock2Status::DeviceList::available_video_number() const;



%ignore casia::device::status::dock2Status::DeviceList::coexist_video_number_max(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DeviceList::coexist_video_number_max();
%rename("%s") casia::device::status::dock2Status::DeviceList::coexist_video_number_max() const;



%ignore casia::device::status::dock2Status::DeviceList::camera_list(std::vector<casia::device::status::dock2Status::CameraList>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::DeviceList::camera_list() const;
%template(CameraList_vector) std::vector<casia::device::status::dock2Status::CameraList>;


%template(_DeviceListSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::DeviceList, std::false_type>;
%template(DeviceListSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DeviceList, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::DeviceList, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::DeviceList& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::LiveCapacity
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::LiveCapacity::LiveCapacity(casia::device::status::dock2Status::LiveCapacity&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::LiveCapacity::available_video_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveCapacity::available_video_number();
%rename("%s") casia::device::status::dock2Status::LiveCapacity::available_video_number() const;



%ignore casia::device::status::dock2Status::LiveCapacity::coexist_video_number_max(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveCapacity::coexist_video_number_max();
%rename("%s") casia::device::status::dock2Status::LiveCapacity::coexist_video_number_max() const;



%ignore casia::device::status::dock2Status::LiveCapacity::device_list(std::vector<casia::device::status::dock2Status::DeviceList>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveCapacity::device_list() const;
%template(DeviceList_vector) std::vector<casia::device::status::dock2Status::DeviceList>;


%template(_LiveCapacitySeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::LiveCapacity, std::false_type>;
%template(LiveCapacitySeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LiveCapacity, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LiveCapacity, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::LiveCapacity& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::LiveStatusItem
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::LiveStatusItem::LiveStatusItem(casia::device::status::dock2Status::LiveStatusItem&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::LiveStatusItem::video_id(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveStatusItem::video_id();
%rename("%s") casia::device::status::dock2Status::LiveStatusItem::video_id() const;



%ignore casia::device::status::dock2Status::LiveStatusItem::video_type(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveStatusItem::video_type();
%rename("%s") casia::device::status::dock2Status::LiveStatusItem::video_type() const;



%ignore casia::device::status::dock2Status::LiveStatusItem::video_quality(casia::device::status::dock2Status::VideoQuality&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveStatusItem::video_quality();
%rename("%s") casia::device::status::dock2Status::LiveStatusItem::video_quality() const;



%ignore casia::device::status::dock2Status::LiveStatusItem::status(casia::device::status::dock2Status::LiveStreamingState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveStatusItem::status();
%rename("%s") casia::device::status::dock2Status::LiveStatusItem::status() const;



%ignore casia::device::status::dock2Status::LiveStatusItem::error_status(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveStatusItem::error_status();
%rename("%s") casia::device::status::dock2Status::LiveStatusItem::error_status() const;



%template(_LiveStatusItemSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::LiveStatusItem, std::false_type>;
%template(LiveStatusItemSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LiveStatusItem, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LiveStatusItem, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::LiveStatusItem& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::LiveStatus
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::LiveStatus::LiveStatus(casia::device::status::dock2Status::LiveStatus&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::LiveStatus::live_status(std::vector<casia::device::status::dock2Status::LiveStatusItem>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::LiveStatus::live_status() const;
%template(LiveStatusItem_vector) std::vector<casia::device::status::dock2Status::LiveStatusItem>;


%template(_LiveStatusSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::LiveStatus, std::false_type>;
%template(LiveStatusSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LiveStatus, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::LiveStatus, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::LiveStatus& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::WirelessLink
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::WirelessLink::WirelessLink(casia::device::status::dock2Status::WirelessLink&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::WirelessLink::dongle_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::dongle_number();
%rename("%s") casia::device::status::dock2Status::WirelessLink::dongle_number() const;



%ignore casia::device::status::dock2Status::WirelessLink::four_g_link_state(casia::device::status::dock2Status::LinkState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::four_g_link_state();
%rename("%s") casia::device::status::dock2Status::WirelessLink::four_g_link_state() const;



%ignore casia::device::status::dock2Status::WirelessLink::sdr_link_state(casia::device::status::dock2Status::LinkState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::sdr_link_state();
%rename("%s") casia::device::status::dock2Status::WirelessLink::sdr_link_state() const;



%ignore casia::device::status::dock2Status::WirelessLink::link_workmode(casia::device::status::dock2Status::LinkWorkMode&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::link_workmode();
%rename("%s") casia::device::status::dock2Status::WirelessLink::link_workmode() const;



%ignore casia::device::status::dock2Status::WirelessLink::sdr_quality(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::sdr_quality();
%rename("%s") casia::device::status::dock2Status::WirelessLink::sdr_quality() const;



%ignore casia::device::status::dock2Status::WirelessLink::four_g_quality(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::four_g_quality();
%rename("%s") casia::device::status::dock2Status::WirelessLink::four_g_quality() const;



%ignore casia::device::status::dock2Status::WirelessLink::four_g_uav_quality(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::four_g_uav_quality();
%rename("%s") casia::device::status::dock2Status::WirelessLink::four_g_uav_quality() const;



%ignore casia::device::status::dock2Status::WirelessLink::four_g_gnd_quality(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::four_g_gnd_quality();
%rename("%s") casia::device::status::dock2Status::WirelessLink::four_g_gnd_quality() const;



%ignore casia::device::status::dock2Status::WirelessLink::sdr_freq_band(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::sdr_freq_band();
%rename("%s") casia::device::status::dock2Status::WirelessLink::sdr_freq_band() const;



%ignore casia::device::status::dock2Status::WirelessLink::four_g_freq_band(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::WirelessLink::four_g_freq_band();
%rename("%s") casia::device::status::dock2Status::WirelessLink::four_g_freq_band() const;



%template(_WirelessLinkSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::WirelessLink, std::false_type>;
%template(WirelessLinkSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::WirelessLink, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::WirelessLink, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::WirelessLink& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::MediaFileDetail
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::MediaFileDetail::MediaFileDetail(casia::device::status::dock2Status::MediaFileDetail&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::MediaFileDetail::remain_upload(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::MediaFileDetail::remain_upload();
%rename("%s") casia::device::status::dock2Status::MediaFileDetail::remain_upload() const;



%template(_MediaFileDetailSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::MediaFileDetail, std::false_type>;
%template(MediaFileDetailSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::MediaFileDetail, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::MediaFileDetail, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::MediaFileDetail& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::NetworkState
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::NetworkState::NetworkState(casia::device::status::dock2Status::NetworkState&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::NetworkState::type(casia::device::status::dock2Status::NetworkType&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::NetworkState::type();
%rename("%s") casia::device::status::dock2Status::NetworkState::type() const;



%ignore casia::device::status::dock2Status::NetworkState::quality(casia::device::status::dock2Status::NetworkQuality&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::NetworkState::quality();
%rename("%s") casia::device::status::dock2Status::NetworkState::quality() const;



%ignore casia::device::status::dock2Status::NetworkState::rate(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::NetworkState::rate();
%rename("%s") casia::device::status::dock2Status::NetworkState::rate() const;



%template(_NetworkStateSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::NetworkState, std::false_type>;
%template(NetworkStateSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::NetworkState, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::NetworkState, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::NetworkState& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::SubDevice
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::SubDevice::SubDevice(casia::device::status::dock2Status::SubDevice&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::SubDevice::device_sn(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SubDevice::device_sn();
%rename("%s") casia::device::status::dock2Status::SubDevice::device_sn() const;



%ignore casia::device::status::dock2Status::SubDevice::device_model_key(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SubDevice::device_model_key();
%rename("%s") casia::device::status::dock2Status::SubDevice::device_model_key() const;



%ignore casia::device::status::dock2Status::SubDevice::device_online_status(casia::device::status::dock2Status::DeviceOnlineStatus&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SubDevice::device_online_status();
%rename("%s") casia::device::status::dock2Status::SubDevice::device_online_status() const;



%ignore casia::device::status::dock2Status::SubDevice::device_paired(casia::device::status::dock2Status::DevicePairedStatus&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::SubDevice::device_paired();
%rename("%s") casia::device::status::dock2Status::SubDevice::device_paired() const;



%template(_SubDeviceSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::SubDevice, std::false_type>;
%template(SubDeviceSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::SubDevice, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::SubDevice, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::SubDevice& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}

////////////////////////////////////////////////////////
// Binding for class casia::device::status::dock2Status::dock2RealTimeStatus
////////////////////////////////////////////////////////

// Ignore overloaded methods that have no application on Python
// Otherwise they will issue a warning
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::dock2RealTimeStatus(casia::device::status::dock2Status::dock2RealTimeStatus&&);

// Overloaded getter methods shadow each other and are equivalent in python
// Avoid a warning ignoring all but one
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::home_position_is_valid(casia::device::status::dock2Status::HomePositionValidity&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::home_position_is_valid();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::home_position_is_valid() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::heading(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::heading();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::heading() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::rtcm_info(casia::device::status::dock2Status::RTCMInfo&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::rtcm_info();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::rtcm_info() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::wireless_link_topo(casia::device::status::dock2Status::WirelessLinkTopo&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::wireless_link_topo();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::wireless_link_topo() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::air_conditioner(casia::device::status::dock2Status::AirConditioner&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::air_conditioner();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::air_conditioner() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::air_transfer_enable(bool&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::air_transfer_enable();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::air_transfer_enable() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::silent_mode(casia::device::status::dock2Status::SilentMode&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::silent_mode();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::silent_mode() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::user_experience_improvement(casia::device::status::dock2Status::UserExperienceImprovement&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::user_experience_improvement();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::user_experience_improvement() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::dongle_infos(std::vector<casia::device::status::dock2Status::DongleInfo>&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::dongle_infos() const;
%template(DongleInfo_vector) std::vector<casia::device::status::dock2Status::DongleInfo>;


%ignore casia::device::status::dock2Status::dock2RealTimeStatus::drone_battery_maintenance_info(casia::device::status::dock2Status::DroneBatteryMaintenanceInfo&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::drone_battery_maintenance_info();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::drone_battery_maintenance_info() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::maintain_status(casia::device::status::dock2Status::MaintainStatus&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::maintain_status();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::maintain_status() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::position_state(casia::device::status::dock2Status::PositionState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::position_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::position_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::emergency_stop_state(casia::device::status::dock2Status::EmergencyStopState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::emergency_stop_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::emergency_stop_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::drone_charge_state(casia::device::status::dock2Status::DroneChargeState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::drone_charge_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::drone_charge_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::backup_battery(casia::device::status::dock2Status::BackupBattery&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::backup_battery();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::backup_battery() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::alarm_state(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::alarm_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::alarm_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::battery_store_mode(casia::device::status::dock2Status::BatteryStoreMode&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::battery_store_mode();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::battery_store_mode() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::activation_time(int64_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::activation_time();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::activation_time() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::height(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::height();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::height() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::alternate_land_point(casia::device::status::dock2Status::AlternateLandPoint&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::alternate_land_point();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::alternate_land_point() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::compatible_status(casia::device::status::dock2Status::FirmwareConsistency&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::compatible_status();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::compatible_status() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::acc_time(int64_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::acc_time();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::acc_time() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::first_power_on(int64_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::first_power_on();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::first_power_on() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::storage(casia::device::status::dock2Status::StorageCapacity&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::storage();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::storage() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::working_current(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::working_current();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::working_current() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::working_voltage(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::working_voltage();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::working_voltage() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::humidity(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::humidity();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::humidity() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::temperature(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::temperature();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::temperature() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::environment_temperature(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::environment_temperature();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::environment_temperature() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::wind_speed(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::wind_speed();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::wind_speed() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::rainfall(casia::device::status::dock2Status::Rainfall&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::rainfall();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::rainfall() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::live_capacity(casia::device::status::dock2Status::LiveCapacity&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::live_capacity();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::live_capacity() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::live_status(casia::device::status::dock2Status::LiveStatus&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::live_status();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::live_status() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::wireless_link(casia::device::status::dock2Status::WirelessLink&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::wireless_link();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::wireless_link() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::media_file_detail(casia::device::status::dock2Status::MediaFileDetail&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::media_file_detail();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::media_file_detail() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::job_number(uint32_t&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::job_number();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::job_number() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::drone_in_dock(bool&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::drone_in_dock();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::drone_in_dock() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::network_state(casia::device::status::dock2Status::NetworkState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::network_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::network_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::supplement_light_state(casia::device::status::dock2Status::SupplementaryLightState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::supplement_light_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::supplement_light_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::cover_state(casia::device::status::dock2Status::CoverState&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::cover_state();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::cover_state() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::sub_device(casia::device::status::dock2Status::SubDevice&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::sub_device();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::sub_device() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::flighttask_step_code(casia::device::status::dock2Status::FlightTaskStepCode&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::flighttask_step_code();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::flighttask_step_code() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::mode_code(casia::device::status::dock2Status::DockModeCode&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::mode_code();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::mode_code() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::firmware_upgrade_status(casia::device::status::dock2Status::FirmwareUpgradeStatus&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::firmware_upgrade_status();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::firmware_upgrade_status() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::firmware_version(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::firmware_version();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::firmware_version() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::latitude(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::latitude();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::latitude() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::longitude(double&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::longitude();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::longitude() const;



%ignore casia::device::status::dock2Status::dock2RealTimeStatus::dock_sn(std::string&&);


// Overloaded getter methods shadow each other and are equivalent in python
// Const accesors produced constant enums instead of arrays/dictionaries when used
// We ignore them to prevent this
%ignore casia::device::status::dock2Status::dock2RealTimeStatus::dock_sn();
%rename("%s") casia::device::status::dock2Status::dock2RealTimeStatus::dock_sn() const;



%template(_dock2RealTimeStatusSeq) eprosima::fastdds::dds::LoanableTypedCollection<casia::device::status::dock2Status::dock2RealTimeStatus, std::false_type>;
%template(dock2RealTimeStatusSeq) eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::dock2RealTimeStatus, std::false_type>;
%extend eprosima::fastdds::dds::LoanableSequence<casia::device::status::dock2Status::dock2RealTimeStatus, std::false_type>
{
    size_t __len__() const
    {
        return self->length();
    }

    const casia::device::status::dock2Status::dock2RealTimeStatus& __getitem__(size_t i) const
    {
        return (*self)[i];
    }
}


// Include the class interfaces
%include "dock2RealTimeStatus.hpp"

// Include the corresponding TopicDataType
%include "dock2RealTimeStatusPubSubTypes.i"

